<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>IntegrationLogging_INT</Name>
	<SourceCode>
		<Declaration>
      <![CDATA[
/// <summary>
/// Logging class for fo_integration
/// </summary>
public class IntegrationLogging_INT
{
    protected guid identifier;
    protected guid runIdentifier;
    protected guid relatedRequest;
    protected IntegrationName_INT integrationName;
    protected DataAreaId company;
    protected utcdatetime beginDatetime;
    protected SysApplicationInsightsTelemetryLogger telemtryLogger;
    protected IntegrationParameters_INT integrationParameters;
    protected IntegrationDirection_INT direction;
    
   
  
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>New</Name>
				<Source>
          <![CDATA[
    /// <summary>
    /// Default new
    /// </summary>
    /// <param name = "_integrationName">Name of the integration</param>
    /// <param name = "_identifier">The identifier of the request</param>
    /// <param name = "_runIdentifier">the runidentifier of the request</param>
    /// <param name = "_company">the company requests to process the integration</param>
    /// <param name = "_relatedRequest">Related request</param>
    protected void New(IntegrationName_INT _integrationName,guid _identifier, guid _runIdentifier,dataareaId _company, guid _relatedRequest, IntegrationDirection_INT _direction)
    {
        
        identifier = _identifier;
        relatedRequest = _relatedRequest;
        integrationName = _integrationName;
        company = _company;
        runIdentifier = _runIdentifier;
        telemtryLogger = SysApplicationInsightsTelemetryLogger::instance();
        integrationParameters = IntegrationParameters_INT::find();
        direction = _direction;
    
        
    }

]]></Source>
			</Method>
			<Method>
				<Name>LogComplete</Name>
				<Source><![CDATA[
    /// <summary>
    /// Registeres a complete event
    /// </summary>
    /// <param name = "_response">The response to log</param>
    public void LogComplete(IntegrationReturnable_INT _response)
    {

        this.logTelemetry('Complete');
        UserConnection userCon = new UserConnection();
        IntegrationRequestData_INT requestData;
        IntegrationRequest_INT request;
        requestData.setConnection(userCon);
        request.setConnection(userCon);

        update_recordset requestData
            setting Output = _response.Serialize(), Ended = DateTimeUtil::utcNow(), State = IntegrationState_INT::Completed
            where requestData.RunIdentifier == RunIdentifier;
        update_recordset request
            setting State = IntegrationState_INT::Completed
            where request.Identifier == identifier;
    }

]]></Source>
			</Method>
			<Method>
				<Name>LogDelete</Name>
				<Source><![CDATA[
    /// <summary>
    /// Deletes the request
    /// </summary>
    /// <param name = "_response">The response to log</param>
    public void LogDelete(IntegrationReturnable_INT _response)
    {
        IntegrationRequestData_INT requestData;
        IntegrationRequest_INT request;

        //No UserConnection due to cascading deletes
        ttsbegin;
        delete_from requestData
            where requestData.RunIdentifier == RunIdentifier;
        delete_from request
            where request.Identifier == identifier;
        ttscommit;
    }

]]></Source>
			</Method>
			<Method>
				<Name>LogErrorFromResponse</Name>
				<Source><![CDATA[
    /// <summary>
    /// Registeres a complete event
    /// </summary>
    /// <param name = "_response">The response to log</param>
    public void LogErrorFromResponse(IntegrationReturnable_INT _response)
    {

        this.logTelemetry('Error');
        UserConnection userCon = new UserConnection();
        IntegrationRequestException_INT requestException;
        IntegrationRequest_INT request;
        IntegrationRequestData_INT requestData;
        requestException.setConnection(userCon);
        request.setConnection(userCon);
        requestData.setConnection(userCon);
                
        requestException.DateTimeStamp = DateTimeUtil::utcNow();
        requestException.Identifier = identifier;
        requestException.RunIdentifier = runIdentifier;
        requestException.Exception = _response.parmException();
        requestException.Stacktrace = _response.parmStacktrace();
        requestException.Infolog = IntegrationLogging_INT::retrieveInfologMessages();
        requestException.insert();

        update_recordset requestData
            setting Output = _response.Serialize(), Ended = DateTimeUtil::utcNow(), State = IntegrationState_INT::Failed
            where requestData.RunIdentifier == RunIdentifier;
        update_recordset request
            setting State = IntegrationState_INT::Failed
            where request.Identifier == identifier;
    }

]]></Source>
			</Method>
			<Method>
				<Name>logError</Name>
				<Source><![CDATA[
    /// <summary>
    /// Registeres an error event
    /// </summary>
    /// <param name = "_ex">The exception to reqister</param>
    public void logError(System.Exception _ex)
    {
        this.logTelemetry('Error');
        UserConnection userCon = new UserConnection();
        IntegrationRequestException_INT requestException;
        IntegrationRequest_INT request;
        IntegrationRequestData_INT requestData;
        requestException.setConnection(userCon);
        request.setConnection(userCon);
        requestData.setConnection(userCon);
                
        requestException.DateTimeStamp = DateTimeUtil::utcNow();
        requestException.Identifier = identifier;
        requestException.RunIdentifier = runIdentifier;
        requestException.setValuesFromException(_ex);
        requestException.insert();
            
        update_recordset request
            setting State = IntegrationState_INT::Failed
            where request.Identifier == identifier;

        update_recordset requestData
            setting Ended = DateTimeUtil::utcNow(), State = IntegrationState_INT::Failed
            where requestData.RunIdentifier == runIdentifier;
    }

]]></Source>
			</Method>
			<Method>
				<Name>initLog</Name>
				<Source><![CDATA[
    /// <summary>
    /// Inits a log with the payload
    /// </summary>
    /// <param name = "_payload">The payload to register</param>
    public void initLog(str _payload)
    {       
        this.logTelemetry('Begin');
        beginDatetime = DateTimeUtil::utcNow();
       
        IntegrationRequest_INT request;
        UserConnection userCon = new UserConnection();
        request.setConnection(userCon);
        userCon.ttsbegin();
        select firstonly forupdate request
            where request.identifier == identifier;
        if(!request)
        {
            request.Identifier = identifier;
            request.DateTimeStamp = beginDatetime;
            request.Company = company;
            request.IntegrationName = integrationName;
            request.State = IntegrationState_INT::New;
            request.RelatedRequest = relatedRequest;
            request.Direction = direction;
            request.insert();
        }
        else
        {
            request.ExecutionCount++;
            request.update();

        }
        userCon.ttscommit();
        IntegrationRequestData_INT requestData;
        requestData.setConnection(userCon);
        requestData.Identifier = identifier;
        requestData.Begin = beginDatetime;
        requestData.RunIdentifier = runIdentifier;
        requestData.Input = _payload;
        requestData.State = IntegrationState_INT::New;
        requestData.insert();

    }

]]></Source>
			</Method>
			<Method>
				<Name>logAsyncSchedule</Name>
				<Source><![CDATA[
    /// <summary>
    /// Logs an AsyncSheduleEvent
    /// </summary>
    public void logAsyncSchedule()
    {
        this.logTelemetry('AsyncSchedule');
        UserConnection userCon = new UserConnection();
 
        IntegrationRequest_INT request;
        IntegrationRequestData_INT requestData;
        request.setConnection(userCon);
        requestData.setConnection(userCon);
 
        update_recordset request
            setting State = IntegrationState_INT::AwaitingAsyncProcessing
            where request.Identifier == identifier;
        update_recordset requestData
            setting Schedulet = DateTimeUtil::utcNow(),State = IntegrationState_INT::AwaitingAsyncProcessing
            where requestData.RunIdentifier == this.runIdentifier;

                

    }

]]></Source>
			</Method>
			<Method>
				<Name>logProcessing</Name>
				<Source><![CDATA[
    /// <summary>
    /// Logs that the integration has begun processing
    /// </summary>
    public void logProcessing()
    {
        this.logTelemetry('Processing');
        UserConnection userCon = new UserConnection();
 
        IntegrationRequest_INT request;
        IntegrationRequestData_INT requestData;
        request.setConnection(userCon);
        requestData.setConnection(userCon);
 
        update_recordset request
            setting State = IntegrationState_INT::Processing
            where request.Identifier == identifier;
        
        update_recordset requestData
            setting Processing = DateTimeUtil::utcNow(),State = IntegrationState_INT::Processing
            where requestData.RunIdentifier == this.runIdentifier;
    
    }

]]></Source>
			</Method>
			<Method>
				<Name>logParsedData</Name>
				<Source><![CDATA[
    public void logParsedData(str _parsed)
    {
        UserConnection userCon = new UserConnection();
 
        IntegrationRequestData_INT requestData;
 
        requestData.setConnection(userCon);
 
        update_recordset requestData
            setting Parsed = _parsed
            where requestData.RunIdentifier == this.runIdentifier;

    }

]]></Source>
			</Method>
			<Method>
				<Name>logKeywords</Name>
				<Source><![CDATA[
    /// <summary>
    /// Updates the requets with the keywords
    /// </summary>
    /// <param name = "_keyword1">Keyword1</param>
    /// <param name = "_keyword2">Keyword2</param>
    /// <param name = "_keyword3">Keyword3</param>
    public void logKeywords(str _keyword1,str _keyword2,str _keyword3)
    {
        UserConnection userCon = new UserConnection();
 
        IntegrationRequest_INT request;
 
        request.setConnection(userCon);
 
        update_recordset request
            setting Keyword1 = _keyword1,Keyword2 = _keyword2,Keyword3 = _keyword3
            where request.Identifier == identifier;
    }

]]></Source>
			</Method>
			<Method>
				<Name>construct</Name>
				<Source><![CDATA[
    /// <summary>
    /// Contructor for logging
    /// </summary>
    /// <param name = "_integration">The integration to handle logging for</param>
    /// <param name = "_runIdentifier">The runIdentifier for the request</param>
    /// <returns>The logging instance</returns>
    public static IntegrationLogging_INT construct(Integration_INT _integration, guid _runIdentifier)
    {
        return new IntegrationLogging_INT(_integration.parmIntegrationSetup().IntegrationName,_integration.parmIdentifier(),_runIdentifier,_integration.parmCompany(),_integration.parmRelatedRequest(), _integration.Direction());
    }

]]></Source>
			</Method>
      <Method>
        <Name>logTelemetry</Name>
        <Source>
          <![CDATA[
   private void logTelemetry(str _status)
    {       
        if (integrationParameters.TelemetryDisable)
        {
            return;
        }

        if (!integrationParameters.TelemetryUseReflection)
        {
            this.logTelemetryNoReflection(_status);
        }
        else
        {
            DictClass dictClass = new DictClass(classNum(SysApplicationInsightsEventTelemetry));

            var event = SysApplicationInsightsEventTelemetry::newFromEventIdName('ATP-Integration','ATP-Integration');

            dictClass.callObject(methodStr(SysApplicationInsightsEventTelemetry, addProperty), event, IntegrationInsightsCorrelationId_INT::newFromValue(guid2Str(this.identifier)));
            dictClass.callObject(methodStr(SysApplicationInsightsEventTelemetry, addProperty), event, IntegrationInsightsIntegration_INT::newFromValue(this.integrationName));
            dictClass.callObject(methodStr(SysApplicationInsightsEventTelemetry, addProperty), event, IntegrationInsightsStatus_INT::newFromValue(_status));

            telemtryLogger.trackEvent(event);
        }
    }

]]>
        </Source>
      </Method>
      <Method>
        <Name>logTelemetryNoReflection</Name>
        <Source>
          <![CDATA[
    private void logTelemetryNoReflection(str _status)
    {
        telemtryLogger.trackEvent(
            SysApplicationInsightsEventTelemetry::newFromEventIdName('ATP-Integration','ATP-Integration')
                .addProperty(IntegrationInsightsCorrelationId_INT::newFromValue(guid2Str(this.identifier)))
                .addProperty(IntegrationInsightsIntegration_INT::newFromValue(this.integrationName))
                .addProperty(IntegrationInsightsStatus_INT::newFromValue(_status)));
    }

]]>
        </Source>
      </Method>     
      
      
      
			<Method>
				<Name>retrieveInfologMessages</Name>
				<Source><![CDATA[
   /* private void logTelemetry(str _status)
    {
        IntegrationInsightsCorrelationId_INT id = IntegrationInsightsCorrelationId_INT::newFromValue(guid2Str(this.identifier));
        IntegrationInsightsIntegration_INT integration = IntegrationInsightsIntegration_INT::newFromValue(this.integrationName);
        IntegrationInsightsStatus_INT status = IntegrationInsightsStatus_INT::newFromValue(_status);
        
        SysApplicationInsightsEventTelemetry event = SysApplicationInsightsEventTelemetry::newFromEventIdName('ATP-Integration','ATP-Integration');
        event.addProperty(id);
        event.addProperty(integration);
        event.addProperty(status);

        telemtryLogger.trackEvent(event);

    }
    */

    /// <summary>
    /// Retrieves all messages from the infolog, concatenating them into a single string.
    /// The resulting string contains each infolog message followed by a carriage return and newline.
    /// </summary>
    public static str retrieveInfologMessages()
    {
        str         result = "";
        container   infologContainer, infologMessage;
        int         i, messageCount;
        int         lastLine = infolog.line();
        str         msgText;
        int         messageType;
 
        if (lastLine == 0)
        {
            return "No infolog messages.";
        }
 
        // Copy all messages from line 1 to the last line
        infologContainer = infolog.copy(1, lastLine);
        messageCount = conlen(infologContainer);
 
        for (i = 1; i <= messageCount; i++)
        {
            infologMessage = conpeek(infologContainer, i);
            messageType = conpeek(infologMessage, 1); // The first element is message type
            msgText = conpeek(infologMessage, 2);     // The second element is the message text
 
            // Append each message followed by a newline
            // If you plan to send via email or store in a table, this formatting can be adjusted
            result += msgText + "\r\n";
        }
 
        return result;
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>